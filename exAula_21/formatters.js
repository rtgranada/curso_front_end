import { format } from 'date-fns'
import { parseISO } from 'date-fns'

export function formatDate(data) {
  console.log(data)
  // console.log(parseISO(data))
  //console.log(format(parseISO(data), 'dd/MM/yyyy'))
}
